# Architecture

Prerequisite: read `docs/README.md` and the two spec docs it references.

## Module layout

Seven Gradle subprojects, each one Java module under `org.hiero.clpr.relay.<suffix>`:

|          Module          |    Suffix     |                                                                                                             Role                                                                                                              |
|--------------------------|---------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `clpr-relay-proto`       | `proto`       | `.proto` files + PBJ-generated types for the gRPC sync protocol and on-chain state shapes. Spec §1 defines the wire forms.                                                                                                    |
| `clpr-relay-core`        | `core`        | Pure interfaces and value types: `TransactionSubmitter`, `ContractStateReader`, `BundlePayloadCodec`, `BundleTrimmer`, `BundleConstructor`, `PeerSyncClient`, `PeerEndpointCache`, `CommitmentLevel`, `ParsedBundle`. No I/O. |
| `clpr-relay-evm`         | `evm`         | The only ledger-aware module. Implements `TransactionSubmitter` and `ContractStateReader` against an EVM JSON-RPC node. ABI codec and ECDSA signer live here.                                                                 |
| `clpr-relay-grpc-server` | `grpc.server` | Helidon-hosted PBJ gRPC server (`ClprGrpcServer`, `ClprEndpointServiceImpl`). Implements the spec's endpoint-to-endpoint sync RPCs. Hosts `ClprSyncHandler` and `ThrottleEnforcer` (inbound bundle limits).                   |
| `clpr-relay-grpc-client` | `grpc.client` | Outbound peer client (`ClprEndpointClient`). Wraps the gRPC stub used by `sync` to call remote endpoints.                                                                                                                     |
| `clpr-relay-sync`        | `sync`        | Orchestration: per-channel sync loops, peer selection. No JSON-RPC or gRPC wire details — depends on `core` abstractions and `grpc-client` for outbound peer calls.                                                           |
| `clpr-relay-app`         | `app`         | Entry point, YAML config loader, dependency wiring, Prometheus metrics endpoint.                                                                                                                                              |

Dependency direction: `app → {sync → grpc-client, grpc-server, evm} → core → proto`. `sync` depends on `grpc-client` for outbound peer calls; `grpc-client` and `grpc-server` do not depend on each other.

## Entry point and wiring

- `clpr-relay-app/.../ClprRelayMain.java` — main method. Loads config, builds metrics, calls `RelayInstance.build(config, metrics)`, starts metrics HTTP server, calls `relay.start()`.
- `clpr-relay-app/.../RelayInstance.java` — explicit constructor wiring, no DI framework. Builds a three-tier hierarchy that mirrors the config: a `LocalNetworkAdapter` per `localNetworks[]` entry (shared JSON-RPC client + gas strategy + CometBFT client), a `ClprServiceHandler` per `clprServices[]` entry (owns its `ChannelDiscoveryTask` and its channel handlers), and a `ClprChannelHandler` per channel (owns that channel's `EvmContractStateReader`, a per-channel enqueuing view — `AccountTransactionSubmitter.forContract(serviceAddress)` — of the network's shared per-account `AccountTransactionSubmitter`, `BundleConstructor`, `LedgerConfigurationPayloadProvider`, `PeerEndpointCache`, `ThrottleEnforcer`, inbound codec, and the two worker loops it spawns). The relay-global pieces are the gRPC server (`ClprGrpcServer` + `ClprSyncHandler`), the peer client (`ClprEndpointClient`), and the metrics registry. The inbound gRPC paths reach channel-scoped state through resolver adapters (`ChannelMappedTransactionSubmitter` and friends) that look a channel handler up by id across every `ClprServiceHandler`.

`start()` starts the gRPC server, then each `ClprServiceHandler` (which registers its predefined channels and launches discovery); each `ClprChannelHandler` spawns its own sync + state-change virtual threads. `stop()` is invoked from a JVM shutdown hook and tears the hierarchy down.

## Key abstractions (read these signatures first when navigating code)

All in `clpr-relay-core/.../`:

|          Type          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `TransactionSubmitter` | `submitBundle(ClprChannel, ParsedBundle) → void`. Enqueues a verified peer bundle onto the signing account's serial `AccountTransactionSubmitter`; the outcome (skip/submit/revert/fail) is surfaced through metrics, never returned or thrown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ContractStateReader`  | Reads `ClprChannel`, `ClprLedgerConfiguration`, individual messages at a chosen `CommitmentLevel`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `CommitmentLevel`      | `LATEST` / `SAFE` / `FINALIZED`. Maps directly to EVM block tags. Per-channel in config.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `BundleConstructor`    | Build the local proof to send to a peer. Currently `StubBundleConstructor` — real verifier-contract integration TBD.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `BundlePayloadCodec`   | Decode an inbound peer bundle into queue metadata + messages via `decodeBundle(bytes)` (no client-side crypto — the proof is verified on chain). `parseBundle(bytes, receivedMessageId)` additionally drops messages the local ledger has already accepted and re-encodes the proof, so an already-delivered message can't sink the bundle as a replay; the chain-agnostic message selection lives in `BundleTrimmer`, the format-specific re-encode in the codec. Re-encoding is skipped (bytes returned verbatim) when nothing is trimmed. Implemented for `QbftProofCodec`, `SeiProofCodec`; `HieroProofCodec` submits untrimmed — re-encoding a block-stream StateProof is out of scope. A codec instance is **bound to one channel** (its channelId is supplied at construction, so no method takes it): built per-channel by `ClprChannelHandler`, held directly by that channel's `ChannelSyncTask`; `ClprSyncHandler` (which serves all channels) resolves the right one per request via a `BundlePayloadCodecResolver` (`ChannelMappedProofCodec`, which looks the owning channel handler up by id — a resolver, not itself a codec). |
| `PeerSyncClient`       | Outbound sync RPC abstraction. `GrpcPeerSyncClient` implements it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `PeerEndpointCache`    | In-memory map of `accountId → ClprEndpoint`. Seeded from the on-chain per-Channel peer roster when the `ClprChannelHandler` is built and replaced wholesale by the channel's state-change poller (`EvmChannelStateChangeTask`) when the channel's `peer_config_timestamp` advances (spec §2.4.2). Reads observe atomic snapshots.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `FailState`            | Pure two-state (`HEALTHY ↔ FAILING`) failure machine for the per-channel loops: streak-driven capped backoff + full-then-compact log decision. No IO, no clock. Owned by each loop task.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Data flow

### Outbound (we send messages from local ledger to peer)

1. The channel's `EvmChannelStateChangeTask` polls `getChannel(channelId)` and observes `nextMessageId` advanced past `ackedMessageId`.
2. `ChannelSyncTask` (running on a virtual thread per channel) wakes, reads the new messages via `EvmContractStateReader`, and asks `BundleConstructor` to build a `ClprSyncPayload`.
3. `PeerSelector` picks a peer endpoint (priorities: never-failed > recent-success > rotated round-robin among the rest — see `PeerStats`).
4. `GrpcPeerSyncClient` invokes the peer's `sync` RPC. Response carries the peer's reciprocal proof (which the peer has just submitted on its own chain).

### Inbound (peer sends us a bundle to land on our chain)

1. Peer calls our `sync` RPC. `ClprEndpointServiceImpl` routes to `ClprSyncHandler`.
2. `ClprSyncHandler` decodes the proof into a `ParsedBundle` (`BundlePayloadCodec`), trimming messages our ledger has already accepted, and calls the submitter's `submitBundle`.
3. The submitter ABI-encodes `submitBundle(channelId32, payload)`, signs the EVM tx client-side as EIP-1559 (type-2), and submits via `eth_sendRawTransaction`. Polls `eth_getTransactionReceipt` until mined or timeout.
4. Response to the peer carries our local proof so the peer can ack progress.

## Threading model

- Each `ClprChannelHandler` owns its own two virtual threads and holds a handle to each: one `ChannelSyncTask` (sync loop, started only when `StartOptions.outboundSync()` is true) and one `EvmChannelStateChangeTask` (state poller, always started). The handler spawns both in `start(outboundSync)` — tagging each thread's log context with the channel id — and signals both to stop in `stop()`. Each task captures its own thread when `run()` starts; `stop()` flips the task's flag and self-interrupts that thread, so a blocking poll/backoff sleep wakes promptly. There is no central `SyncOrchestrator` / `EvmStateChangeListener` orchestrating threads across channels; the handler is the sole owner (the aggregate loop-failure gauges are registered once by `RelayInstance`, computed at scrape time from every live handler's `FailState`).
- On `CLOSED`, both the sync task and the state-change task self-stop instead of polling the dead channel forever; the handler remains registered in its `ClprServiceHandler` (which also keeps discovery from re-registering an already-known — including already-closed — channel).
- Each `ClprServiceHandler` keeps **at most one channel handler per channel id**: `addChannel` ignores (and logs) a duplicate registration, so the predefined-registration and discovery paths cannot double-wire a channel.
- **Both loops are failure-resilient.** Neither `ChannelSyncTask.run()` nor `EvmChannelStateChangeTask.run()` can die on an unchecked per-iteration exception. Each task owns a `FailState` (in `core`) — a pure two-state machine (`HEALTHY ↔ FAILING`) that, on each failure, advances an episode-wide streak, returns a **capped, exponentially-growing backoff** (base and cap from `relay.backoff`, default 1 s → 30 s — never the tight cadence), and decides logging: the **first occurrence of each fault fingerprint** logs at ERROR with the full stacktrace, **repeats** log a compact ERROR line (`#streak` + `channelId` + fingerprint). The **first clean iteration** resets the streak (and fingerprint set) in one step and logs a single recovery INFO. The backoff is an interruptible `Thread.sleep`, so a `stop()` interrupt wakes the task mid-backoff. Peer-RPC failures inside `executeSyncCycle` stay **peer-scoped** (handled by `PeerStats` backoff) and never escalate the whole channel. Backoff bounds are configured by `relay.backoff.{baseMs,capMs}`.
- Helidon's WebServer for inbound gRPC.
- All shared state (`PeerEndpointCache`, and the per-account `AccountTransactionSubmitter` bounded queue drained by a single worker thread) is protected by explicit locks or concurrent collections — there is no global event loop.

## New components (added 2026-05)

|           Component           |    Module     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
|-------------------------------|---------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `AccountTransactionSubmitter` | `evm`         | Per-`(network, signing account)` serial submitter (in `clpr-relay-evm` — it needs the JSON-RPC client). A single worker virtual thread drains **bounded per-channel FIFO queues round-robin** (one request per channel per turn, so no channel starves another), driving each request to a definite outcome before the next, so at most one transaction per account is in flight — which is why it needs no nonce tracking (it reads a fresh `latest` nonce once per request). Gates every paid submission on a gas-free `eth_call` of `submitBundle` run at send time (always on): a revert → skip (`sync.bundle.skipped{reason=rejected}`). That preview is also the true duplicate gate — a stale bundle the pull (`ChannelSyncTask`) and push (`ClprSyncHandler`) paths would both deliver reverts (it is previewed only after the first has mined, since the worker is serial). As queue hygiene, an exact-bundle-bytes re-offer already pending for a channel is suppressed at enqueue (`evm.tx.queue_deduped`) so those two paths cannot fill the FIFO with identical copies and drop a genuinely-new bundle; this is content-exact, so unlike the removed counter-keyed fingerprint it never suppresses a distinct PAUSE-recovery bundle. Only an empty **ACTIVE** initial-state bundle is dropped without a preview (`sync.bundle.skipped{reason=empty}`) — a status-only close at those same counters is not empty and is always submitted so the lifecycle advances. No coalescing (order preserved per channel); a full per-channel queue drops that channel's new request (`evm.tx.queue_dropped`) without affecting others. The chain is the sole arbiter, so a transient failure is retried at the same nonce (each round re-reading the live market fee and re-pricing up if it rose, clamped to the cap, to clear a base-fee spike); a request that never reaches a verdict is abandoned after a `MAX_SUBMIT_ROUNDS` safety valve (`evm.tx.failures{reason=abandoned}`) so one stuck bundle cannot wedge the worker — the sync loop re-drives it. |
| `ThrottleEnforcer`            | `grpc.server` | Enforces inbound bundle size and message-count limits (`maxSyncBytes`, `maxMessagesPerBundle`, `maxMessagePayloadBytes`) per spec §3.1.1; oversized/over-count bundles are skipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `RelayProtocol`               | `core`        | Holds `PROTOCOL_VERSION` constant (= 1). Each channel validates it at registration (in `ClprChannelHandler.create`); a mismatch fails that channel's registration (logged, skipped) without downing the relay.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## What is NOT here

- No verifier-contract integration. Proof construction/verification are stubs.
- No on-chain event subscriptions. Each channel's `EvmChannelStateChangeTask` polls.
- No persistent state. Everything is reconstructable from on-chain reads on restart; `PeerStats` etc. is in-memory only.
- No bundling/batching across channels. Each channel's task is independent.
